import { createDemoChatResult } from "./demo-note";
describe("createDemoChatResult", () => { it("creates deterministic Skill demo content", () => { let index = 0; const ids = () => `id-${++index}`; const result = createDemoChatResult("什么是 Skill？", ids); expect(result.mode).toBe("demo"); expect(result.note.question).toBe("什么是 Skill？"); expect(result.note.blocks.filter((block) => block.type === "text" || block.type === "bullet-list").length).toBeGreaterThanOrEqual(2); expect(result.note.blocks.some((block) => block.type === "flow-diagram" && block.nodes.length === 3)).toBe(true); expect(JSON.stringify(result.note)).toContain("highlight"); }); });
